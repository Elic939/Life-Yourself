# 审查与修复记录

独立只读审查发现以下问题，均补充失败复现测试后修复并复验：打开饮水面板可能丢失餐食草稿；删除失败重试成功后旧列表未刷新；放弃草稿与在途保存竞态；日志缺少有氧分钟；累计数值溢出；损坏数据库启动恢复缺少预览确认。

窗口检查修复关闭事件重入、编辑面板底层误操作、旧滚动位置、表单可访问名称、导航选中状态。

发布更新检查初次失败于进程退出后 DLL 短暂占用；等待退出后的释放再操作。原启动、更新和全量数据比较断言保留。

原始红/绿结果位于 artifacts/test-results；最终全套结果见 all-tests.trx。

# 实施决策

- 使用 WPF / .NET 10 原生窗口，SQLite 主文件，JSON 备份；不启本地 Web 服务。
- 数据位于系统 LocalApplicationData/PersonalLife/life.db；所有测试使用独立临时目录。
- 自包含 win-x64 解压包；程序更新不操作数据目录。
- 新项目在当前用户指定目录建立本地 Git 仓库与 develop 分支；没有现有分支或代码需要隔离，不建立额外工作树。
- 按用户要求顺序实施、逐阶段测试；不删除失败用例，不以未发现测试视为成功。
- 人工重启电脑不自动执行，不将进程重开冒称为机器重启验证。

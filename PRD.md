# 个人生活管理 App 产品需求文档（PRD）

- 版本：V1.1
- 整理日期：2026-09-12
- 用途：作为后续页面细化、开发和验收的依据。
- 当前阶段：需求与页面框架设计；尚未开始开发。
- 确认状态：产品目标、七个模块、第一版范围及排除项已获用户确认。页面结构已提出并提供可点击预览；预览的具体视觉样式、留白和控件位置尚未得到用户明确确认，不作为最终视觉定稿。
- 本文中的保存失败、恢复校验等规则，是为满足已确认的数据可靠性目标而补充的可验收要求，不代表功能已经实现。

## 1. 产品目标和使用场景

为用户本人提供一个集中管理日常生活的 App，把日常事项、健身、饮食、游戏娱乐和每日复盘放在同一个地方，减少重复记录，便于查看当天安排与历史情况。

### 使用限制

- 作为 Windows 本地桌面 App 运行，安装或解压后可通过应用入口打开独立窗口，支持断网使用。
- 日常使用不需要打开浏览器、输入网址或运行命令。
- 无须登录、账户、云数据库或外部服务。
- 用户数据留在当前电脑，不上传网络。
- 关闭并重新打开 App、重启电脑后，已保存的数据必须保留。
- 不制作浏览器网站，不需要网站部署，不开发手机端。

### 典型使用场景

1. 开始一天时，在首页查看今天的事项、训练、餐食和娱乐安排。
2. 白天完成事项，并在相应模块记录训练、实际饮食和游戏时间。
3. 晚上参考当天摘要写总结日志。
4. 按日期回看过去的计划和实际记录，必要时修正。
5. 定期导出备份，在需要时从本机备份文件恢复数据。

## 2. 整体页面框架和导航结构

采用左侧固定导航、右侧页面内容的电脑端布局。

左侧依次提供以下七个入口：

1. 首页总览
2. 今日计划
3. 健身计划
4. 饮食计划
5. 游戏娱乐
6. 总结日志
7. 数据与设置

右侧顶部显示当前页面名称。除“数据与设置”外，各页面提供日期选择、前一天、后一天和“回到今天”操作；初次打开默认显示当天。

- 页面切换保留当前查看日期；点击摘要时进入对应模块的同一日期。
- 切换日期只切换查看内容，不移动、覆盖或修改原记录。
- 无记录时显示简短说明和添加入口，不自动填入演示数据。
- 简单操作在页面内完成，字段较多的添加和编辑通过右侧面板完成。
- 实际日期按当前电脑的本地日期解释；跨午夜的游戏记录需要保留完整起止日期时间。

具体色彩、字体、卡片尺寸、页面留白及编辑面板细节将在下一阶段定稿。

## 3. 首页内容结构

首页用于看清当天状态和快速进入模块，不承担所有详细信息的录入。

| 区域 | 第一版内容 | 可执行操作 |
| --- | --- | --- |
| 日期和今日概况 | 当前查看日期、日常事项已完成数与总数、接下来的安排 | 切换日期、回到今天 |
| 今日计划摘要 | 当天事项、时间、完成状态，重要事项提示 | 勾选或取消完成；进入今日计划 |
| 健身摘要 | 训练主题、计划时间、主要动作、记录进度或休息日 | 进入当天健身计划 |
| 饮食摘要 | 早餐、午餐、晚餐及加餐安排，实际记录状态，饮水摘要 | 进入当天饮食计划 |
| 游戏娱乐摘要 | 游戏名称、计划时段、预计时长、当天实际游戏总时长 | 进入当天游戏娱乐 |
| 总结日志摘要 | 当天是否已有日志、心情（如已填写） | 新建或编辑当天日志 |

首页的事项完成比例只统计日常事项，不将用餐、训练和日志混成同一套勾选任务。模块没有数据时显示“尚未安排”或“尚未记录”，避免把缺少记录误判为未完成。

## 4. 模块用途及第一版最必要的数据和操作

### 4.1 今日计划

**用途：** 管理日常待办，并集中查看来自各生活模块的当天安排。

最必要的数据：

- 所属日期、事项名称。
- 可选安排时间、优先级、完成状态。
- 用于稳定区分事项的记录标识。

最必要的操作：

- 添加、编辑、删除事项。
- 设置或修改日期、时间、优先级。
- 完成或取消完成。
- 按时间展示事项，未指定时间的事项单独放置；已完成事项可折叠。
- 查看当天训练、用餐、娱乐的简要入口，并进入对应模块编辑。
- 按日期回看历史事项。

### 4.2 健身计划

**用途：** 安排训练并记录实际训练量，支持力量与按时长记录的运动。

最必要的数据：

- 训练日期、主题、可选开始时间和预计时长；当天可标记休息日。
- 动作名称和记录类型（力量或按时长记录）。
- 力量动作计划：组数、次数、可选重量。
- 力量动作实际记录：每组次数、可选重量。
- 按时长记录的运动：计划时长、实际时长。
- 训练或动作的完成状态。

最必要的操作：

- 新建、编辑、删除训练安排和动作，设置休息日。
- 逐组填写或修正实际次数、重量，填写实际运动时长。
- 标记训练完成，保留计划与实际记录。
- 按日期查看训练历史。

不强制所有运动都填写重量，不把动作简化成普通待办。重量和时长需显示单位，次数、组数和时长不得为负数。

### 4.3 饮食计划

**用途：** 安排每日餐食，记录实际饮食与饮水。

最必要的数据：

- 日期、餐次（早餐、午餐、晚餐、加餐）。
- 计划餐食文本、实际餐食文本、实际记录状态。
- 饮水记录的日期和毫升数。

最必要的操作：

- 按餐次填写、修改或清除计划与实际餐食。
- 使用“照计划记录”将计划复制为实际记录，之后仍可独立修改。
- 添加、修正、删除饮水记录，并显示当天总量。
- 查看历史饮食和饮水记录。

不要求用户选择标准食物数据库或填写热量。修改计划不自动修改已经记录的实际餐食。

### 4.4 游戏娱乐

**用途：** 安排娱乐时间并手动记录实际游戏时长，不在 App 中提供游戏。

最必要的数据：

- 娱乐计划：日期、游戏名称、计划时段、预计时长。
- 实际记录：游戏名称、实际日期，以及起止日期时间或直接填写的时长。
- 当天和本周累计实际时长，由实际记录计算。

最必要的操作：

- 添加、编辑、删除娱乐安排。
- 手动添加、编辑、删除实际游玩记录。
- 支持两种录入方式：填写起止日期时间后计算时长，或直接填写时长。
- 查看每日、每周累计时长，以及当天计划与实际的差异。

同一条记录只使用一种时长计算方式，不重复累计。第一版跨午夜记录按开始日期归属；直接录入时长按用户选择的日期归属。此统计规则应在相关界面简短说明。

### 4.5 总结日志

**用途：** 按日期复盘生活，结合当天客观记录写个人总结。

最必要的数据：

- 日期，一天对应一份日志。
- 可选心情、今日总结、收获、待改进内容。
- 最后修改时间。

最必要的操作：

- 新建、编辑、删除当天日志，允许只填写部分内容。
- 自动保存文字修改，回看历史日志。
- 同页查看当天事项、训练、饮食和娱乐摘要。

摘要来自原模块，不复制成另一份可编辑的记录。不自动生成日志内容。

### 4.6 数据与设置

**用途：** 管理本机数据、备份恢复和必要的使用偏好。

最必要的数据：

- 所有模块记录及其关联日期和记录标识。
- 基础偏好，第一版至少包括一周从星期一或星期日开始。
- 数据格式版本、备份导出时间。

最必要的操作：

- 查看保存状态和本地数据保存方式说明。
- 导出完整备份文件、选择备份文件并恢复。
- 修改基础偏好。
- 明确确认后清空全部用户数据。

## 5. 首页、今日计划和各模块之间的关系

采用“数据在所属模块维护，其他页面读取摘要”的原则。

| 信息 | 维护位置 | 其他页面如何使用 |
| --- | --- | --- |
| 日常事项 | 今日计划 | 首页显示摘要并允许切换完成状态；日志显示完成情况 |
| 训练安排及实际训练 | 健身计划 | 首页显示训练摘要；今日计划显示安排入口；日志显示实际训练摘要 |
| 餐食与饮水 | 饮食计划 | 首页显示餐食及记录状态；今日计划显示餐次入口；日志显示实际记录摘要 |
| 娱乐安排与游戏时长 | 游戏娱乐 | 首页显示计划和实际时长；今日计划显示娱乐安排入口；日志显示实际用时 |
| 日志 | 总结日志 | 首页显示是否已写及心情 |

- 首页修改事项完成状态，必须同步到今日计划，而非产生副本。
- 在模块内修改或删除记录后，首页、今日计划中的相关摘要和日志参考摘要随之更新。
- 今日计划中的模块入口不生成重复待办，也不参与日常事项完成率计算。
- 健身、饮食、娱乐均区分计划和实际；有计划不等于已执行。
- 用餐未设置具体时间时按餐次展示，不凭空安排时间。

## 6. 本地数据文件、保存、备份和恢复要求

### 6.1 日常持久化保存

- 所有用户数据必须持久化在当前电脑，不依赖页面内存、登录或网络。
- 输入和编辑自动保存；显示“保存中”“已保存”或“保存失败”。只有持久化成功才能显示“已保存”。
- 保存失败时保留当前输入、给出可理解的提示并允许重试，不能静默丢弃。
- 切换页面或日期时，必须处理尚未完成的保存；不能因页面切换丢失编辑内容。
- 数据以本机文件形式保存，不依赖浏览器缓存或浏览器用户资料。用户主动删除数据文件或电脑损坏不在普通重启保留的保证范围内，因此必须提供可独立保留的备份文件。

### 6.2 数据文件要求和技术边界

- 完整备份采用可导入的本地 JSON 文件，包含格式版本、导出时间、全部模块数据和基础偏好。
- 建议文件名为 `life-app-backup-YYYY-MM-DD-HHmmss.json`。
- 数据文件不得包含登录凭据或依赖外部链接才能恢复的业务数据。
- 日常主数据必须由桌面 App 自动写入本机数据文件，可采用本地数据库文件或其他可靠文件格式；具体技术在实施前确定。不得使用浏览器存储作为主数据来源。
- 数据目录与程序安装目录分开，更新或替换程序不得覆盖用户数据。设置页显示数据目录的完整路径，并提供“打开数据文件夹”入口。
- 实施前明确实际保存位置和应用启动方式，写入使用说明；“只在内存保存”或“每次关闭前手动导出”不符合要求。
- 日常主数据文件与用户导出的 JSON 备份文件是不同对象；日常保存自动完成，备份由用户主动导出。

### 6.3 导出备份

- 用户主动导出全部数据，而非仅导出当前页面或日期。
- 备份文件保存在用户可定位的本机位置。
- 导出失败需提示，不得提示成功却产生空文件或缺失模块数据。

### 6.4 导入恢复

- 第一版采用整份替换恢复，不做合并。
- 读取备份后先校验格式、版本和必要数据结构，再显示导出时间和各类记录数量。
- 明确说明恢复将替换当前数据，并提供先导出当前数据的入口；用户确认后才执行。
- 用户取消、备份无效或版本不受支持时，保留现有数据。
- 恢复写入失败时保留或回滚至原有完整数据，不允许留下部分替换的状态。
- 恢复成功后，刷新所有页面摘要，重启后仍保持恢复结果。

### 6.5 删除与清空

- 普通条目删除后提供短暂撤销入口。
- 清空全部数据前明确提示范围并要求再次确认；取消后不改变数据。
- 清空操作不删除用户已导出到电脑上的备份文件。

## 7. 第一版必须完成的功能

- 七个页面和固定导航、日期切换、回到今天。
- 首页真实汇总各模块数据，不使用固定演示数字。
- 日常事项的增删改、优先级和完成状态。
- 健身安排、力量逐组记录、按时长运动记录和历史查看。
- 三餐与加餐计划、实际饮食记录、饮水记录与合计。
- 娱乐时间安排、手动游戏时长记录、每日及每周统计。
- 按日期写日志、记录心情和查看当天摘要及历史。
- 模块摘要联动，计划与实际分开保存。
- 本机自动持久保存、保存失败反馈、完整备份和替换恢复。
- 必要的空状态、输入校验、删除撤销和危险操作确认。
- 提供可在 Windows 上安装或解压使用的桌面 App，双击应用入口即可打开独立窗口。
- 清楚说明本地启动方式、数据保存位置、备份和恢复步骤，并提供打开数据文件夹的入口。

## 8. 第一版暂时不做的功能

- 登录、账户、云数据库、联网服务、跨设备同步。
- 浏览器网站、网站部署、手机端、手机适配专项开发。
- 自动识别运行中的游戏、后台监控游戏时长。
- 内置小游戏、社交、复杂成就系统。
- 自动生成健身处方、AI 教练。
- 食物识别、食物数据库、复杂热量和营养计算。
- AI 自动写日志、高级分析报表。
- 系统级后台提醒、外部日历或健康设备集成。
- 备份增量合并、多人协作。

## 9. 可实际检查的验收标准

以下检查必须在实际运行的 App 中完成，设计预览不能代替功能验收。

| 编号 | 检查步骤 | 合格结果 |
| --- | --- | --- |
| A01 本地使用 | 断开网络，双击应用入口启动，切换全部页面并录入数据 | App 以独立窗口打开，核心操作和保存可用，无须浏览器、网址、命令、登录或联网 |
| A02 导航与日期 | 切换七个入口，在过去日期进入模块后返回首页 | 页面名称与选中导航一致；日期保持一致；回到今天有效 |
| A03 初始状态 | 使用空数据启动 | 无演示记录；各模块有清晰空状态及添加入口 |
| A04 日常事项 | 新建带时间和优先级的事项，在首页完成，再进入今日计划 | 同一条事项状态一致，完成数正确；修改和删除后摘要更新 |
| A05 模块区分 | 新建训练、晚餐和娱乐安排，查看今日计划 | 显示相应入口；不产生重复待办，也不增加日常事项总数 |
| A06 力量训练 | 新建 3 组训练，记录两组不同的实际次数和重量 | 每组实际值独立保留，计划不被覆盖，历史可查看 |
| A07 按时长运动 | 新建快走计划并记录实际时长 | 无须填写重量即可保存，计划与实际时长分别显示 |
| A08 饮食 | 为晚餐填写计划，照计划记录后修改实际内容 | 原计划保持不变；餐食记录状态与首页一致 |
| A09 饮水 | 添加 250 ml 和 300 ml，再删除前一条 | 总量先为 550 ml，之后为 300 ml；历史日期不受影响 |
| A10 游戏时长 | 记录 20:00—20:45，再直接记录 30 分钟 | 当天合计 75 分钟，周统计对应增加；编辑和删除后重新计算 |
| A11 跨日游戏 | 记录某日 23:30 至次日 00:30 | 时长为 60 分钟，归属开始日期，无负值和重复累计 |
| A12 日志 | 只填一部分日志并选择心情，等待已保存后切换日期再返回 | 原内容保留；同一天不重复创建日志；首页状态正确 |
| A13 普通重启 | 每个模块各保存一条记录及一项设置，关闭重开 App，再重启电脑后打开 App | 所有数据与设置保持一致，不依赖浏览器用户资料 |
| A14 快速切换 | 修改文字后立即切换模块或日期 | 编辑内容被保存，或得到明确未保存提示；不得静默丢失 |
| A15 保存异常 | 在测试中模拟持久存储写入失败 | 不显示已保存；保留输入并提示失败，解除故障后可重试 |
| A16 备份完整 | 跨多个日期填写各模块数据并导出 JSON 备份 | 文件可读取，包含所有模块、偏好、版本及导出时间 |
| A17 成功恢复 | 导出备份后更改数据，再导入原备份并确认 | 预览时间和数量正确；完整恢复原数据；重开 App 和重启电脑后仍一致 |
| A18 无效备份 | 选择损坏文件或不支持版本的备份 | 提示原因，不改变当前数据 |
| A19 恢复取消或失败 | 在确认前取消；另在测试中模拟恢复写入失败 | 两种情况均保留原有完整数据，无半恢复状态 |
| A20 删除与清空 | 删除普通条目后撤销；清空时先取消，再明确确认 | 撤销可恢复条目；取消清空不改变数据；确认后清空，但导出的备份文件仍在 |
| A21 输入校验 | 提交空事项名称、负饮水量、负时长、结束早于开始的游戏记录 | 显示字段相关提示，阻止无效记录保存 |
| A22 电脑端可用性 | 在桌面 App 中调整窗口大小并逐页操作 | 文字、按钮和表单无遮挡或截断；能用键盘访问主要操作 |
| A23 真实汇总 | 修改各模块样例记录，切换首页和日志摘要 | 数量、时长、记录状态由真实数据计算，不是写死的示例 |
| A24 本机数据文件 | 在设置页查看路径并打开数据文件夹，保存一条记录后重开 App | 能定位真实主数据文件；记录从文件恢复；数据不保存在浏览器缓存中 |
| A25 程序与数据分离 | 在测试中保留数据目录，替换或更新程序文件后启动 | 原记录与设置保留，不被新程序的默认数据覆盖 |

## 10. 后续设计与实施约束

1. 优先确认已展示页面框架的布局，再细化字段、编辑面板和错误状态。
2. 开发前确定 Windows 桌面应用技术、安装包或解压运行的交付形式、主数据文件格式和目录，确保满足 A01、A13、A24 和 A25。具体技术尚未选定。
3. 设计预览中的日期、游戏、餐食和训练数据仅用于示意，不作为用户真实偏好或默认数据。
4. 本文保存不代表用户授权开始编码、创建项目、部署或扩大第一版范围。
